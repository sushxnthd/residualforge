# Contributing

Open an issue before a large change. Keep new behavior covered by tests and include a reproducible example for changes that affect reported metrics.

For local development:

```bash
pip install -e .[dev]
pytest -q
```
